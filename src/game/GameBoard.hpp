#ifndef GAME_BOARD

class GameBoard {
    private:
        /* data */
    public:
        GameBoard(/* args */);
        ~GameBoard();
        // void setUpboard(GameBoard* board);
        void setUpBoard();
};

#endif